

let price  = 0

export function getStockData() {

     // 1. update price (fake market movement)
const change = (Math.random() * 0.2 - 0.1) // -1 to +1
    price += change

//      // 2. update price (fake market movement) this is the change in the price
// const change = (Math.random() * 2 - 1); // -1 to +1
//     price += change.toFixed(2);


  // 2. time snapshot (synchronized with price update)
const now = new Date()

// const times = 

    return {
        name: 'QtechAI',
        sym: 'QTA',
        change: change,
        price: price,
        time: `${now.getHours()}:${String(now.getMinutes()).padStart(2, "0")}:${String(now.getSeconds()).padStart(2, "0")}`

    }
}


// const date = new Date()

// const time = `${date.getHours()}:${String(date.getMinutes()).padStart(2, "0")}:${date.getSeconds()}`



// let price = 100;

// function renderStockTicker() {

//   // 1. update price (fake market movement)
// const change = (Math.random() * 2 - 1); // -1 to +1
//     price += change;

//   // 2. time snapshot (synchronized with price update)
// const now = new Date()

// const time = `${now.getHours()}:${String(now.getMinutes()).padStart(2, "0")}:${String(now.getSeconds()).padStart(2, "0")}`

//   // 3. direction indicator
// const direction =
//     change > 0 ? '🟢' :
//     change < 0 ? '🔴' :
//     '🎱'

//   // 4. output combined ticker
// console.log(`[${time}] Price: $${price.toFixed(2)} ${direction}`)
// }

// setInteral(renderStockTicker, 1000)


// let price = 100;

// export function getStock() {
//   const change = (Math.random() * 2 - 1);
//   price += change;

//   return {
//     price,
//     change
//   };
// }
























































































































































































































































































































































































































































































































