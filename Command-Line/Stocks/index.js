

const  nameImp = document.getElementById("name-imp")
const  symbolImp = document.getElementById("symbol-imp")
const  priceImp = document.getElementById("price-imp")
const  timeImp = document.getElementById("time-imp")
const  arrowImp = document.getElementById("arrow-btn")

import {getStockData } from "./fakeStockApi.js";


const data = getStockData()

let change = data.change
let Price = data.price

console.log(change)

// console.log(data)


nameImp.textContent = `NAME: ${data.name}`
symbolImp.textContent = `SYMBOL: ${data.sym}`

setInterval(() => {

    const data = getStockData();

    
    priceImp.textContent = `PRICE: $${data.price.toFixed(4)}`;
    timeImp.textContent = `TIME: ${data.time}`;


    if (Price < change) {
        arrowImp.className = 'green'

    } else if (Price > change) {
        arrowImp.className = 'red'

    } else {
        arrowImp.className = 'white'
    }

    // compare prices 

    console.log("Price:", Price)
    console.log("Change:", change)


console.log(`$${data.price.toFixed(4)}`)

}, 1000)


// const date = new Date()

// const time = `${date.getHours()}:${String(date.getMinutes()).padStart(2, "0")}:${date.getSeconds()}`
// const newprice = (Math.random() * 3).toFixed(2)

// const originalprice = 5.00


// console.log(time) // the time display
// console.log(newprice) // price  


// function renderStockTicker() {

//     const message =
//     originalprice > newprice ? '🔴' :
//     originalprice < newprice ? '🟢' :
//     '🎱'

// console.log(message)
// }

// renderStockTicker()



// let price = 100;

// function renderStockTicker() {

//   // 1. update price (fake market movement)
// const change = (Math.random() * 2 - 1); // -1 to +1
//     price += change;

//   // 2. time snapshot (synchronized with price update)
// const now = new Date()

// const time = `${now.getHours()}:${String(now.getMinutes()).padStart(2, "0")}:${String(now.getSeconds()).padStart(2, "0")}`

//   // 3. direction indicator
// const direction =
//     change > 0 ? '🟢' :
//     change < 0 ? '🔴' :
//     '🎱'

//   // 4. output combined ticker
// console.log(`[${time}] Price: $${price.toFixed(2)} ${direction}`)
// }

// setInterval(renderStockTicker, 15000)

































































































































































































































































































































































































































































































































